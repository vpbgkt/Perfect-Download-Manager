AWSTemplateFormatVersion: "2010-09-09"
Description: >
  Perfect Download Manager - serverless licensing backend.
  DynamoDB (license records) + two Lambda functions (activate, validate) + HTTP API.
  The ECDSA signing private key lives in SSM Parameter Store (SecureString), not here.

Parameters:
  CodeBucket:
    Type: String
    Description: S3 bucket holding the Lambda deployment package.
  CodeKey:
    Type: String
    Description: S3 key of the Lambda deployment package (zip).
  PrivateKeyParam:
    Type: String
    Default: /pdm/licensing/private-key
    Description: SSM SecureString parameter name holding the EC private key PEM.
  TableName:
    Type: String
    Default: pdm-licenses
  TokenTtlDays:
    Type: Number
    Default: 14
    Description: Signed token lifetime; client must re-validate online within this window.
  PublicKeyBase64:
    Type: String
    Default: "MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEPGc8VP8HPWYtXZx8TCprOaFse9iQTSypZp8pjltwA7cYRSWnNr+sNOKa92/deMvNb5NdIVB3SX1oPexTUKQS2w=="
    Description: >
      ECDSA P-256 public key (SPKI base64) matching the SSM private signing key. Used by the
      deactivate handler to verify the caller's signed token (public-key only; no private key access).
      Must be regenerated together with the signing key on rotation.

Resources:

  LicensesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref TableName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: licenseKey
          AttributeType: S
      KeySchema:
        - AttributeName: licenseKey
          KeyType: HASH
      SSESpecification:
        SSEEnabled: true
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      # Auto-expire ephemeral per-IP rate-limit rows (attribute `ttl`, epoch seconds). Real
      # license/trial rows use an ISO `expiresAt` string and are never touched by TTL.
      TimeToLiveSpecification:
        AttributeName: ttl
        Enabled: true

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: pdm-licensing-access
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                Resource: !GetAtt LicensesTable.Arn
              - Effect: Allow
                Action:
                  - ssm:GetParameter
                Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter${PrivateKeyParam}"
              - Effect: Allow
                Action:
                  - kms:Decrypt
                Resource: "*"
                Condition:
                  StringEquals:
                    kms:ViaService: !Sub "ssm.${AWS::Region}.amazonaws.com"

  ActivateFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: pdm-license-activate
      Runtime: nodejs20.x
      Handler: activate.handler
      MemorySize: 256
      Timeout: 10
      Role: !GetAtt LambdaExecutionRole.Arn
      Code:
        S3Bucket: !Ref CodeBucket
        S3Key: !Ref CodeKey
      Environment:
        Variables:
          LICENSE_TABLE: !Ref TableName
          PRIVATE_KEY_PARAM: !Ref PrivateKeyParam
          TOKEN_TTL_DAYS: !Ref TokenTtlDays

  ValidateFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: pdm-license-validate
      Runtime: nodejs20.x
      Handler: validate.handler
      MemorySize: 256
      Timeout: 10
      Role: !GetAtt LambdaExecutionRole.Arn
      Code:
        S3Bucket: !Ref CodeBucket
        S3Key: !Ref CodeKey
      Environment:
        Variables:
          LICENSE_TABLE: !Ref TableName
          PRIVATE_KEY_PARAM: !Ref PrivateKeyParam
          TOKEN_TTL_DAYS: !Ref TokenTtlDays

  TrialFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: pdm-license-trial
      Runtime: nodejs20.x
      Handler: trial.handler
      MemorySize: 256
      Timeout: 10
      Role: !GetAtt LambdaExecutionRole.Arn
      Code:
        S3Bucket: !Ref CodeBucket
        S3Key: !Ref CodeKey
      Environment:
        Variables:
          LICENSE_TABLE: !Ref TableName
          PRIVATE_KEY_PARAM: !Ref PrivateKeyParam
          TRIAL_DAYS: "14"

  # Minimal least-privilege role for /deactivate: DynamoDB read/write only. It verifies tokens with
  # the PUBLIC key (from env), so unlike the signing functions it needs NO SSM/KMS access.
  DeactivateExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: pdm-deactivate-access
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:UpdateItem
                Resource: !GetAtt LicensesTable.Arn

  DeactivateFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: pdm-license-deactivate
      Runtime: nodejs20.x
      Handler: deactivate.handler
      MemorySize: 256
      Timeout: 10
      Role: !GetAtt DeactivateExecutionRole.Arn
      Code:
        S3Bucket: !Ref CodeBucket
        S3Key: !Ref CodeKey
      Environment:
        Variables:
          LICENSE_TABLE: !Ref TableName
          PUBLIC_KEY_B64: !Ref PublicKeyBase64

  HttpApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: pdm-licensing-api
      ProtocolType: HTTP

  ActivateIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt ActivateFunction.Arn
      PayloadFormatVersion: "2.0"

  ValidateIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt ValidateFunction.Arn
      PayloadFormatVersion: "2.0"

  ActivateRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: "POST /activate"
      Target: !Sub "integrations/${ActivateIntegration}"

  ValidateRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: "POST /validate"
      Target: !Sub "integrations/${ValidateIntegration}"

  TrialIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt TrialFunction.Arn
      PayloadFormatVersion: "2.0"

  TrialRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: "POST /trial"
      Target: !Sub "integrations/${TrialIntegration}"

  DeactivateIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt DeactivateFunction.Arn
      PayloadFormatVersion: "2.0"

  DeactivateRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: "POST /deactivate"
      Target: !Sub "integrations/${DeactivateIntegration}"

  ApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref HttpApi
      StageName: "$default"
      AutoDeploy: true
      # Protect the backend from abuse/brute-force with conservative throttling.
      DefaultRouteSettings:
        ThrottlingBurstLimit: 20
        ThrottlingRateLimit: 10

  ActivatePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ActivateFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/activate"

  ValidatePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ValidateFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/validate"

  TrialPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref TrialFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/trial"

  DeactivatePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref DeactivateFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/deactivate"

Outputs:
  ApiBaseUrl:
    Description: Base URL for the licensing API. Set as PdmLicensing.ApiBaseUrl in the client.
    Value: !GetAtt HttpApi.ApiEndpoint
  TableName:
    Value: !Ref TableName
